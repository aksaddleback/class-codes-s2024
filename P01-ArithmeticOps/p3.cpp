#include<iostream> 
using namespace std; 
int main() { 
    cout << "Enter length and width of the rectangle: "; 
    double l, w; 
    cin >> l >> w; 
    // double area = l * w; 
    cout << "Area of the rectangle is: " << l * w << endl; 
}